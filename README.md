# Portfolio — BUT Informatique (S6)

Site statique personnel. Hébergé via GitHub Pages.

## Modifier le contenu
- Édite `index.html` (sections déjà en place) et `style.css`.
- Remplace les blocs marqués `[ ... ]` / classe `todo` par ton contenu personnel.
- Ajoute ta photo dans `assets/photo.jpg` et décommente la balise `<img>` dans le hero.
- Dépose ton CV nommé `cv.pdf` à la racine.

## Déploiement
Repo `marewane-fertikh.github.io` → Settings ▸ Pages ▸ Source: branche `main` / dossier `/ (root)`.
URL publique : https://marewane-fertikh.github.io
